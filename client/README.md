Mini SaaS Template Store — Frontend

How to run (PowerShell):

    cd "C:\Users\dell\Desktop\full-stack-intern-task\client"
    npm install
    npm run dev

Notes:
- This frontend keeps authentication token in memory only (no localStorage) as requested.
- Backend expected at http://localhost:3000 (see server in `server/`).
